# Marketplace Frontend Test Cases

| TestCase ID | Module | Page/Component Name | Description | Preconditions | Steps | Expected Result | Actual Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| MP-FE-001 | Marketplace | Marketplace Page Shell | Verify the marketplace route opens and renders the top header, search box, type chips, lab chips, sidebar, and results grid. | Frontend app is running and the user can access `/marketplace`. | 1. Navigate to `/marketplace`.<br>2. Verify the title, search input, type chips, lab chips, sidebar sections, and results area render. | The marketplace page loads with all implemented page-level UI sections visible and usable. |  |
| MP-FE-002 | Marketplace | Marketplace Navigation Entry | Verify users can reach the marketplace from implemented entry points tied to frontend navigation. | Frontend app is running. | 1. Open `/`.<br>2. Use navbar and landing-page marketplace links to navigate to `/marketplace`. | All implemented frontend links into the marketplace route navigate correctly. |  |
| MP-FE-003 | Marketplace | Loading State | Verify the marketplace shows the implemented loading text while models are still being fetched. | Network conditions or mocking delay the models request. | 1. Open `/marketplace` with delayed models API.<br>2. Verify `Loading models...` appears before data resolves. | The page shows the implemented loading label during fetch and switches to the final count after load completes. |  |
| MP-FE-004 | Marketplace | Model Card Rendering | Verify each marketplace model card displays the expected data-driven fields and optional badge behavior. | Marketplace model data is loaded. | 1. Inspect multiple model cards.<br>2. Verify icon, name, org, description, tags, rating, price, and optional badge behavior. | Model cards render the implemented data fields correctly, including conditional badge rendering. |  |
| MP-FE-005 | Marketplace | Model Card Hover State | Verify the model card hover interaction updates elevation and CTA styling. | Marketplace grid is visible on a pointer-enabled device. | 1. Hover over a model card.<br>2. Verify lift, shadow, and CTA style changes.<br>3. Move pointer away. | Hovering a model card triggers the implemented visual emphasis and resets when hover ends. |  |
| MP-FE-006 | Marketplace | Search Input | Verify marketplace search filters cards by model name or description text. | Marketplace model data is loaded. | 1. Search by model keyword.<br>2. Search by description keyword.<br>3. Clear search. | Search narrows results using the implemented name/description text matching and resets when cleared. |  |
| MP-FE-007 | Marketplace | Type Chips | Verify each type chip filters the listing according to the implemented `types` or open-source logic. | Marketplace model data is loaded. | 1. Click each type chip.<br>2. Validate the filtered result set.<br>3. Reset with `All`. | Type chips apply the implemented per-type filter logic and `All` resets the chip filter. |  |
| MP-FE-008 | Marketplace | Lab Chips | Verify lab chip selection filters the results and supports toggling the active lab off. | Marketplace model data is loaded. | 1. Select a lab chip.<br>2. Verify filtered results.<br>3. Toggle it off and reset with `All Labs`. | Lab chips filter by lab name, can be toggled off, and `All Labs` resets the lab selection. |  |
| MP-FE-009 | Marketplace | Provider Sidebar Filter | Verify the Provider checkbox group narrows results to selected organizations and supports multi-select behavior. | Marketplace model data is loaded. | 1. Select one provider.<br>2. Select a second provider.<br>3. Clear both. | Provider checkboxes apply the implemented multi-select organization filter and clear cleanly. |  |
| MP-FE-010 | Marketplace | Pricing Sidebar Filter | Verify pricing checkboxes filter models using the implemented Free, Pay-per-use, and Subscription logic. | Marketplace model data is loaded. | 1. Test Free, Pay-per-use, and Subscription separately.<br>2. Test combined selections. | Pricing filters match the implemented price-string logic for free, pay-per-use, and subscription products. |  |
| MP-FE-011 | Marketplace | Min Rating Filter | Verify Min Rating pills reduce the result set based on model rating thresholds and can be reset to All. | Marketplace model data is loaded. | 1. Apply 4.0+ and 4.5+.<br>2. Reset with `All`. | The Min Rating control applies the implemented threshold filter and resets correctly. |  |
| MP-FE-012 | Marketplace | License Filter | Verify license checkboxes behave according to the implemented logic, including the Open Source filter tied to free models. | Marketplace model data is loaded. | 1. Apply `Open Source`.<br>2. Observe behavior for `Commercial` and `Research Only`.<br>3. Clear selections. | The license filter behaves exactly as implemented, with `Open Source` affecting results and other options not breaking the page. |  |
| MP-FE-013 | Marketplace | Combined Filters | Verify search plus chips plus sidebar filters work together and reduce the listing cumulatively. | Marketplace model data is loaded. | 1. Apply search and multiple filters.<br>2. Observe cumulative result reduction. | Combined marketplace controls apply cumulatively and only show cards matching the current overall filter state. |  |
| MP-FE-014 | Marketplace | Empty State And Clear Filters | Verify an impossible filter combination shows the empty state and `Clear all filters` resets every marketplace control. | Marketplace model data is loaded. | 1. Apply zero-result filters.<br>2. Verify empty state.<br>3. Click `Clear all filters`. | The empty state appears when no models match and the reset action restores all filters and results. |  |
| MP-FE-015 | Marketplace | Quick Guides Section | Verify the static Quick Guides section renders all guide labels and remains visually stable. | User is on `/marketplace`. | 1. Inspect `Quick Guides`.<br>2. Click each row. | All implemented Quick Guide rows render correctly and clicking them does not break the marketplace UI. |  |
| MP-FE-016 | Marketplace | Model Detail Dialog Open And Close | Verify clicking a marketplace card CTA opens the dialog and all supported close mechanisms work. | Marketplace model grid is visible. | 1. Open dialog.<br>2. Validate close button, backdrop, and Escape behavior.<br>3. Verify scroll lock while open. | The detail dialog opens from a model card and closes correctly by button, backdrop, and keyboard. |  |
| MP-FE-017 | Marketplace | Model Detail Dialog Tabs | Verify each detail-dialog tab is reachable and renders its tab-specific content. | A model detail dialog is open. | 1. Click all tabs.<br>2. Verify active styling and content changes. | Every implemented tab is selectable and shows the matching content panel. |  |
| MP-FE-018 | Marketplace | Prompt Guide Copy Action | Verify Prompt Guide copy buttons copy the prompt text and show the temporary copied state. | A model detail dialog is open on the `Prompt Guide` tab and clipboard permissions are available. | 1. Click `Copy` on a prompt block.<br>2. Verify `Copied!` state.<br>3. Verify reset after timeout. | Copy actions write the expected text to clipboard and show the transient copied state. |  |
| MP-FE-019 | Marketplace | Agent Creation Tab Navigation | Verify the `Open Agent Builder` action in the dialog routes to `/agents`. | A model detail dialog is open on the `Agent Creation` tab. | 1. Click `Open Agent Builder`.<br>2. Verify dialog close and route change. | The Agent Creation tab routes users to the implemented Agent Builder page. |  |
| MP-FE-020 | Marketplace | API Failure Fallback | Verify marketplace remains usable when the models API fails because the hook falls back to cached models or fallback data. | The models request can be forced to fail. | 1. Simulate `/models` failure.<br>2. Open `/marketplace`.<br>3. Verify cards still render from fallback data. | Marketplace does not crash on API failure and falls back to locally available model data as implemented. |  |
| MP-FE-021 | Marketplace | Large Result Set Layout | Verify the card grid layout remains usable when a large number of products are available. | Model data set is expanded through fixtures or mocks. | 1. Load a large marketplace data set.<br>2. Scroll through the grid.<br>3. Verify no pagination or infinite-scroll controls appear. | The marketplace grid remains readable and functional with a large data set, and no nonexistent pagination UI appears. |  |
| MP-FE-022 | Marketplace | Conditional Rendering By Auth State | Verify marketplace content is available to both guests and authenticated users, with only shared-shell auth controls changing. | One guest/logged-out state and one authenticated state are available. | 1. Open `/marketplace` while logged out or as guest.<br>2. Reopen while authenticated.<br>3. Compare content and navbar auth controls. | Marketplace content does not become restricted by auth state, while only the shared navbar auth controls change. |  |
