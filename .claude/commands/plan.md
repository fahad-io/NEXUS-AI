# Plan Command

- Goal:
  - Inspect repo state before changing code.
- Steps:
  - Read shared rules and specs first.
  - Check `frontend/package.json` and `backend/package.json` before assuming scripts.
  - Outline FE, BE, QA, and analyzer work items.
  - Keep scope limited to requested changes.
- Current script notes:
  - Frontend: `dev`, `build`, `start`, `lint`
  - Backend: `build`, `start`, `start:dev`, `start:prod`, `lint`, `test`, `test:e2e`

