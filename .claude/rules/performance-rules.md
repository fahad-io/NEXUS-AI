# Performance Rules

- Avoid unnecessary client-side fetching.
- Stream or paginate large model lists when needed.
- Keep first-load UI responsive on desktop and mobile.
- Defer non-critical panels and charts if they block interaction.
- Cache model metadata when safe.
- Avoid duplicate API requests for the same view state.
- Measure bundle and API impact before adding dependencies.

