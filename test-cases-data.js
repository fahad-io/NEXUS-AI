const frontend_test_cases = [];
const backend_test_cases = [];

frontend_test_cases.push(
  {
    testCaseId: 'FE-001',
    module: 'Landing Page',
    description: 'Verify the landing page renders the hero, action chips, featured models, newsletter form, and footer links.',
    preconditions: 'Frontend app is running and user opens `/`.',
    steps: [
      '1. Navigate to `/`.',
      '2. Verify the sticky navbar, hero heading, hero input, and `Ask AI` button render.',
      '3. Verify action chips such as `Create image`, `Generate audio`, and `Just exploring` render.',
      '4. Verify featured model cards render in the Featured Models section.',
      '5. Verify the newsletter email field and `Subscribe free` button render.',
      '6. Verify footer links for `Models` and `Research`, plus `API`, `Privacy`, and `Terms` buttons, render.',
    ],
    expectedResult: 'All expected landing page sections and controls render without layout breakage.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-002',
    module: 'Navbar',
    description: 'Verify navbar navigation links and active-state behavior across main routes.',
    preconditions: 'Frontend app is running.',
    steps: [
      '1. Open `/chat` and verify `Chat Hub` is highlighted.',
      '2. Open `/marketplace` and verify `Marketplace` is highlighted.',
      '3. Open `/discover` and verify `Discover New` is highlighted.',
      '4. Open `/agents` and verify `Agents` is highlighted.',
      '5. Click the NexusAI brand and verify navigation back to `/`.',
    ],
    expectedResult: 'Navbar links navigate correctly and the active route is visually indicated for each page.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-003',
    module: 'Navbar Language Selector',
    description: 'Verify language selector opens, allows switching languages, and updates HTML direction for RTL languages.',
    preconditions: 'User is on any page that includes the navbar.',
    steps: [
      '1. Click the language pill in the navbar.',
      '2. Verify the dropdown lists the configured languages.',
      '3. Select `English` and verify `document.documentElement.lang` becomes `en` and direction remains LTR.',
      '4. Re-open the dropdown and select `العربية` or `اردو`.',
      '5. Verify `document.documentElement.dir` changes to `rtl` and the dropdown closes.',
    ],
    expectedResult: 'Language selection updates the current language indicator and applies the expected `lang` and `dir` attributes.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-004',
    module: 'Landing Newsletter Form',
    description: 'Verify newsletter form client-side validation for blank and invalid email values.',
    preconditions: 'User is on `/`.',
    steps: [
      '1. Leave the newsletter input blank and click `Subscribe free`.',
      '2. Verify an error toast appears asking for an email address.',
      '3. Enter an invalid email such as `abc` and submit again.',
      '4. Verify an error toast appears asking for a valid email.',
      '5. Enter a valid email such as `qa@example.com` and submit.',
      '6. Verify a success toast appears and the field resets.',
    ],
    expectedResult: 'Blank and invalid input are rejected client-side, while valid input shows a success toast and clears the field.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-005',
    module: 'Login Page',
    description: 'Verify login page renders all fields, actions, and guest entry.',
    preconditions: 'User is logged out and opens `/auth/login`.',
    steps: [
      '1. Navigate to `/auth/login`.',
      '2. Verify Email and Password fields render with required attributes.',
      '3. Verify the `Remember me` checkbox renders.',
      '4. Verify `Sign In` and `Continue as Guest` buttons render.',
      '5. Verify the `Sign up` link routes to `/auth/signup`.',
    ],
    expectedResult: 'The login page renders the expected controls and navigation options for auth and guest access.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-006',
    module: 'Login And Signup',
    description: 'Verify login success, login failure, signup success, and signup password-length validation.',
    preconditions: 'A valid user account exists for login and a new email is available for signup.',
    steps: [
      '1. Attempt login with invalid credentials and verify the inline error banner appears.',
      '2. Log in with valid credentials and verify redirect to `/dashboard`.',
      '3. Open `/auth/signup` and enter a password shorter than 6 characters.',
      '4. Verify client-side password length validation blocks submission.',
      '5. Submit valid signup data with a new email.',
      '6. Verify redirect to `/dashboard` after successful signup.',
    ],
    expectedResult: 'Auth screens handle success and failure states exactly as implemented by the hooks and page logic.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-007',
    module: 'Guest Session Entry',
    description: 'Verify `Continue as Guest` creates a guest session and routes the user to the Chat Hub.',
    preconditions: 'User is logged out.',
    steps: [
      '1. Open `/auth/login`.',
      '2. Click `Continue as Guest`.',
      '3. Verify navigation to `/chat`.',
      '4. Verify a guest session banner appears.',
      '5. Verify guest state is stored in session-based auth state.',
    ],
    expectedResult: 'A guest session is created and the user lands in Chat Hub with guest-specific messaging.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-008',
    module: 'Chat Hub Layout',
    description: 'Verify the chat page renders the expected three-panel desktop layout and composer controls.',
    preconditions: 'User is on `/chat` with a desktop viewport.',
    steps: [
      '1. Navigate to `/chat`.',
      '2. Verify the left model panel renders.',
      '3. Verify the center conversation area renders.',
      '4. Verify the right insights/actions panel renders.',
      '5. Verify the composer contains textarea, send action, file upload, image upload, camera, and microphone controls.',
    ],
    expectedResult: 'The Chat Hub renders the expected 3-column workspace and input controls.',
    actualResult: '',
  }
);

frontend_test_cases.push(
  {
    testCaseId: 'FE-009',
    module: 'Guest Banner',
    description: 'Verify the guest banner shows countdown text, sign-in link, and dismiss behavior for guest users only.',
    preconditions: 'User is in guest mode and on `/chat`.',
    steps: [
      '1. Verify the guest banner is visible above the chat layout.',
      '2. Verify the banner shows remaining time text and a sign-in link.',
      '3. Click the dismiss action and verify the banner disappears.',
      '4. Sign in and return to `/chat`.',
      '5. Verify the guest banner no longer renders for authenticated state.',
    ],
    expectedResult: 'The guest banner appears only for guest users, can be dismissed, and is hidden for authenticated users.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-010',
    module: 'Chat Messaging',
    description: 'Verify sending a text message appends user and assistant messages and creates a recent session entry.',
    preconditions: 'User is on `/chat` and the chat API is reachable.',
    steps: [
      '1. Type a text message into the composer.',
      '2. Click the send action.',
      '3. Verify the user message appears in the thread.',
      '4. Verify an assistant response appears after processing.',
      '5. Verify the session appears in Recent Chats with a title derived from the first user message.',
    ],
    expectedResult: 'Text messaging updates the active session, renders both messages, and updates recent session metadata.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-011',
    module: 'Chat Sessions',
    description: 'Verify selecting a saved session from Recent Chats loads the matching messages and model context.',
    preconditions: 'Authenticated user has at least two saved sessions.',
    steps: [
      '1. Open `/chat`.',
      '2. Click one session in the Recent Chats list.',
      '3. Verify its messages load in the conversation area.',
      '4. Verify the active model indicator updates to that session model.',
      '5. Click a different saved session.',
      '6. Verify the thread and model context change to the second session.',
    ],
    expectedResult: 'Selecting a saved session loads the correct thread and associated model state.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-012',
    module: 'Chat Attachments And Camera',
    description: 'Verify file upload adds an attachment chip and the camera modal supports photo/video mode controls.',
    preconditions: 'User is on `/chat`; a supported file is available; camera permission can be granted.',
    steps: [
      '1. Upload a supported file and verify an attachment chip appears before sending.',
      '2. Open the camera modal from the composer.',
      '3. Verify a live preview plus `Photo` and `Video` mode toggles render.',
      '4. Switch between modes and verify `Snap` or `Record` controls update accordingly.',
      '5. Close the modal and send a message with the uploaded attachment.',
    ],
    expectedResult: 'Attachment upload and camera modal controls behave as implemented and remain usable in the composer flow.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-013',
    module: 'Marketplace',
    description: 'Verify marketplace search, chips, sidebar filters, and empty-state reset behavior.',
    preconditions: 'User is on `/marketplace` and model data is loaded.',
    steps: [
      '1. Enter a search keyword into the marketplace search box.',
      '2. Apply a type chip such as `Code` or `Vision`.',
      '3. Apply a lab chip and a provider checkbox.',
      '4. Set a minimum rating filter.',
      '5. Verify the count and visible cards update after each change.',
      '6. Force a zero-result combination and verify the no-results state appears.',
      '7. Click `Clear all filters` and verify the full listing returns.',
    ],
    expectedResult: 'Marketplace filtering follows the implemented logic and the empty-state reset restores the full model set.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-014',
    module: 'Model Detail Dialog',
    description: 'Verify model detail dialog opens, supports all tabs, and closes through Escape and close button.',
    preconditions: 'User is on `/marketplace`.',
    steps: [
      '1. Open any model detail dialog.',
      '2. Verify header content shows model name, org, and badge.',
      '3. Switch through `Overview`, `How to Use`, `Pricing`, `Prompt Guide`, `Agent Creation`, and `Reviews`.',
      '4. Press `Escape` to close the dialog.',
      '5. Reopen it and close it through the explicit close button.',
    ],
    expectedResult: 'Dialog tab switching and close behavior work correctly for all supported interactions.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-015',
    module: 'Discover And Agents',
    description: 'Verify Discover feed cards render and the Agents page shows banner, templates, and empty My Agents state.',
    preconditions: 'Frontend app is running.',
    steps: [
      '1. Open `/discover` and verify research cards, category chips, and links render.',
      '2. Open `/agents` and verify the `New Agent` CTA renders.',
      '3. Verify the help banner renders with guide and dismiss actions.',
      '4. Dismiss the banner and verify it hides.',
      '5. Verify template cards render with `Use template` actions.',
      '6. Verify the `My Agents` section shows the empty state.',
    ],
    expectedResult: 'Discover and Agents routes render their implemented content and static actions without layout or state issues.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-016',
    module: 'Dashboard Routes',
    description: 'Verify dashboard overview, history, settings, and billing routes enforce auth and render their implemented content.',
    preconditions: 'Frontend app is running; user can log in.',
    steps: [
      '1. While logged out, open `/dashboard`, `/dashboard/history`, `/dashboard/settings`, and `/dashboard/billing` and verify redirect to `/auth/login`.',
      '2. Log in and open `/dashboard`; verify stat cards, charts, and quick-start links render.',
      '3. Open `/dashboard/history`; verify sessions load, `Continue` navigates to `/chat?session=...`, and `Delete` removes a row.',
      '4. Open `/dashboard/settings`; verify profile fields load, save shows a toast, and `Delete Account` shows an info toast.',
      '5. Open `/dashboard/billing`; verify current plan summary, plan cards, and invoices render.',
    ],
    expectedResult: 'Protected dashboard routes redirect anonymous users and render the expected authenticated content and actions.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-017',
    module: 'Frontend Auth Refresh',
    description: 'Verify the API client refreshes a stale access token and retries the original protected request.',
    preconditions: 'User has a stale access token but a valid refresh cookie.',
    steps: [
      '1. Simulate an expired access token in local storage.',
      '2. Trigger a protected request such as dashboard stats or chat history.',
      '3. Verify the frontend calls `/api/auth/refresh`.',
      '4. Verify a new access token is stored.',
      '5. Verify the original request succeeds after retry.',
    ],
    expectedResult: 'The frontend recovers from token expiry without forcing the user back to login when refresh is valid.',
    actualResult: '',
  },
  {
    testCaseId: 'FE-018',
    module: 'Frontend Toast And Error States',
    description: 'Verify success, error, and info toasts render and key pages fall back gracefully when data requests fail or return empty results.',
    preconditions: 'Backend responses can be mocked to fail or return empty data.',
    steps: [
      '1. Trigger a success toast via settings save or newsletter submit.',
      '2. Trigger an error toast via invalid newsletter input.',
      '3. Trigger an info toast via landing footer buttons.',
      '4. Force models fetch failure and verify marketplace falls back to local model data.',
      '5. Force empty history data and verify the `No chat sessions yet.` state renders.',
    ],
    expectedResult: 'Toasts render by type and pages show fallbacks or empty states instead of crashing.',
    actualResult: '',
  }
);

backend_test_cases.push(
  {
    testCaseId: 'BE-001',
    module: 'Auth API',
    description: 'Verify `POST /api/auth/signup` creates a new user, returns an access token, and sets the refresh cookie.',
    preconditions: 'No user exists for the submitted email.',
    steps: [
      '1. Send `POST /api/auth/signup` with a valid name, email, and password.',
      '2. Verify the response contains `access_token` and `user`.',
      '3. Verify the returned user includes `id`, `email`, `name`, and `role`.',
      '4. Verify a `refresh_token` cookie is set with path `/api/auth` and `httpOnly`.',
    ],
    expectedResult: 'Signup creates the account, returns auth data, and sets the refresh cookie correctly.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-002',
    module: 'Auth API Validation',
    description: 'Verify signup rejects invalid payloads and duplicate emails.',
    preconditions: 'Backend is running.',
    steps: [
      '1. Send signup with an invalid email format.',
      '2. Send signup with a name shorter than 2 characters.',
      '3. Send signup with a password shorter than 6 characters.',
      '4. Create a valid user once, then resend signup with the same email.',
    ],
    expectedResult: 'Invalid fields are rejected by validation and duplicate email attempts are rejected with a conflict response.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-003',
    module: 'Auth API Login',
    description: 'Verify login success, invalid login rejection, refresh-token rotation, and logout cookie clearing.',
    preconditions: 'A valid user exists and backend auth endpoints are reachable.',
    steps: [
      '1. Send `POST /api/auth/login` with valid credentials and verify `access_token`, `refresh_token`, and `user` are returned.',
      '2. Repeat login with invalid credentials and verify unauthorized rejection.',
      '3. Call `POST /api/auth/refresh` with the valid cookie and verify a new access token is returned.',
      '4. Call `POST /api/auth/logout` and verify the refresh cookie is cleared.',
    ],
    expectedResult: 'Auth endpoints enforce credential checks, refresh valid sessions, and clear refresh state on logout.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-004',
    module: 'Models API',
    description: 'Verify `GET /api/models` returns model data and supports type, lab, search, and id lookups.',
    preconditions: 'Models collection contains seeded model documents.',
    steps: [
      '1. Call `GET /api/models` with no filters.',
      '2. Call `GET /api/models?type=code`, `?type=open`, `?lab=OpenAI`, and `?search=gpt`.',
      '3. Call `GET /api/models/{knownId}`.',
      '4. Call `GET /api/models/non-existent-id`.',
    ],
    expectedResult: 'Model list and detail endpoints return matching data and reject unknown ids.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-005',
    module: 'Chat API - Authenticated Send',
    description: 'Verify authenticated `POST /api/chat/send` creates or updates a user-owned session with user and assistant messages.',
    preconditions: 'User is authenticated with a valid bearer token.',
    steps: [
      '1. Send `POST /api/chat/send` with `message` and `modelId` using bearer auth.',
      '2. Verify the response contains `reply`, `sessionId`, and serialized `session` data.',
      '3. Verify the session contains the user message and the assistant reply.',
      '4. Verify `isGuest` is false.',
    ],
    expectedResult: 'Authenticated chat send persists a non-guest session and returns the updated session payload.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-006',
    module: 'Chat API - Guest Send',
    description: 'Verify guest `POST /api/chat/send` works without bearer auth and returns an expiring guest session.',
    preconditions: 'No bearer token is sent.',
    steps: [
      '1. Send `POST /api/chat/send` without auth and without a session id.',
      '2. Capture the returned `sessionId`.',
      '3. Send another message with the returned guest session id.',
      '4. Verify the session is marked `isGuest: true` and includes `expiresAt`.',
    ],
    expectedResult: 'Guest chat works without authentication and uses guest session ownership semantics.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-007',
    module: 'Chat API Validation',
    description: 'Verify send-message DTO validation rejects blank, oversized, and invalid enum payloads.',
    preconditions: 'Backend is running.',
    steps: [
      '1. Send `POST /api/chat/send` with an empty `message`.',
      '2. Send `POST /api/chat/send` with a message longer than 10000 characters.',
      '3. Send `POST /api/chat/send` with an invalid `type` value.',
      '4. Send an attachment whose `kind` is not one of `image`, `video`, or `file`.',
    ],
    expectedResult: 'Invalid message payloads are rejected by the global validation pipe.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-008',
    module: 'Chat Session API',
    description: 'Verify authenticated session create, get, delete, and history endpoints enforce ownership and return paginated data.',
    preconditions: 'At least one authenticated user exists.',
    steps: [
      '1. Create a session using `POST /api/chat/session` with bearer auth.',
      '2. Fetch it through `GET /api/chat/session/:id` as the same user.',
      '3. Delete it through `DELETE /api/chat/session/:id` as the same user.',
      '4. Call `GET /api/chat/history?page=1&limit=20` and verify `data`, `total`, `page`, `limit`, and `totalPages`.',
      '5. Attempt to fetch or delete the same session as a different user.',
    ],
    expectedResult: 'The owner can manage the session, history is paginated, and cross-user access is denied.',
    actualResult: '',
  }
);

backend_test_cases.push(
  {
    testCaseId: 'BE-009',
    module: 'Dashboard API',
    description: 'Verify dashboard stats, history, and billing endpoints are protected and return aggregated values for the authenticated user.',
    preconditions: 'Authenticated user has chat session activity.',
    steps: [
      '1. Call `GET /api/dashboard/stats` with bearer auth.',
      '2. Verify the response includes `totalRequests`, `totalMessages`, `avgLatency`, `cost`, `modelUsage`, and `activeSessions`.',
      '3. Call `GET /api/dashboard/history` with bearer auth and verify it mirrors chat history shape.',
      '4. Call `GET /api/dashboard/billing` with bearer auth and verify plan, usage, cost, and invoices are present.',
      '5. Repeat one dashboard call without auth.',
    ],
    expectedResult: 'Dashboard endpoints are protected and return the expected aggregated response structures for valid users.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-010',
    module: 'Forms API',
    description: 'Verify public contact and feedback form endpoints accept valid payloads and reject values outside DTO bounds.',
    preconditions: 'Backend is running.',
    steps: [
      '1. Send a valid payload to `POST /api/forms/contact` and verify a success response.',
      '2. Resend contact with invalid email, short name, and short message values.',
      '3. Send a valid payload to `POST /api/forms/feedback` and verify success.',
      '4. Resend feedback with `rating: 0`, `rating: 6`, and an oversized `page` value.',
    ],
    expectedResult: 'Form endpoints are public, accept valid submissions, and reject invalid DTO payloads.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-011',
    module: 'Upload API',
    description: 'Verify `POST /api/upload` accepts allowed file types, returns normalized metadata, and rejects unsupported formats or oversize files.',
    preconditions: 'Supported, unsupported, and over-50-MB test files are available.',
    steps: [
      '1. Upload a supported file such as PDF, PNG, TXT, or WEBM.',
      '2. Verify the response contains `url`, `filename`, `originalName`, `size`, and `type`.',
      '3. Upload an unsupported format and verify a bad-request response.',
      '4. Upload a file larger than 50 MB and verify the request is rejected.',
    ],
    expectedResult: 'Allowed uploads succeed with metadata and blocked uploads fail according to the configured rules.',
    actualResult: '',
  },
  {
    testCaseId: 'BE-012',
    module: 'Security And Session Lifecycle',
    description: 'Verify protected routes reject anonymous access, public routes remain accessible, and guest sessions expose expiry metadata for TTL cleanup.',
    preconditions: 'Backend is running and connected to MongoDB.',
    steps: [
      '1. Call protected endpoints such as `/api/auth/me`, `/api/chat/history`, and `/api/dashboard/stats` without auth and verify rejection.',
      '2. Call public endpoints such as `/api/auth/login`, `/api/auth/signup`, `/api/models`, `/api/forms/contact`, and `/api/upload` without auth and verify they remain reachable.',
      '3. Create a guest chat session through `POST /api/chat/send` without auth.',
      '4. Verify the returned session is marked `isGuest: true` and includes `expiresAt` about 3 hours ahead.',
      '5. Verify the schema is configured with a TTL index on `expiresAt`.',
    ],
    expectedResult: 'Guard behavior matches public/protected route intent and guest sessions expose expiration metadata for cleanup.',
    actualResult: '',
  }
);

module.exports = {
  frontend_test_cases,
  backend_test_cases,
};
